'use strict';

// type variable
var number = 5; // Infinity (4 / 0), NaN (Not a Number ('string' * 9))
var string = "Hello!";
var sym = Symbol();
var boolean = true; // false
null; // переменной не существует (обычно выдается ошибка)
undefined; // неинициализированная переменная
var obj = {};